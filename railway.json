{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "cd backend && python manage.py migrate && gunicorn bills_backend.wsgi:application",
        "restartPolicyType": "ON_FAILURE"
    }
}